import { Adjustment } from "../models/adjustmentModel";

/**
 * Retrieves all adjustments for the current user.
 * Placeholder implementation that returns an empty list.
 *
 * @returns A promise that resolves to an array of Adjustment entities.
 */
export async function getAllAdjustments(): Promise<Adjustment[]> {
  // TODO: Replace with Firestore repository call in a later step.
  return [];
}
